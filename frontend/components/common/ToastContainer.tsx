'use client';

import React from 'react';

export const ToastContainer = () => {
  return <div id="toast-root" className="fixed top-4 right-4 z-50 flex flex-col gap-2 pointer-events-none" />;
};

export default ToastContainer;
