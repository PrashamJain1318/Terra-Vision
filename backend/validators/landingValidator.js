/**
 * Validator helper mock placeholder for Landing API params.
 */
export const queryValidator = (data) => {
  // Enforce type checks on optional query limits if needed
  return { error: null, value: data };
};
